name: zerocks_common
description: Shared models and services for the Zerocks platform.
version: 0.0.1

environment:
  sdk: ^3.9.0

dependencies:
  cloud_firestore: ^6.4.1
  firebase_auth: ^6.5.1
  firebase_storage: ^13.4.1
  firebase_messaging: ^16.2.2
  uuid: ^4.5.3

dev_dependencies:
  flutter_lints: ^5.0.0
